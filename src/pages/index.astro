---
import HeadCommon from '../components/HeadCommon.astro'

import LandingPage from '../components/LandingPage/LandingPage.astro'
import LandingSEO from '../components/LandingPage/LandingSEO.astro'
import type { Frontmatter } from '../config'
import * as CONFIG from '../config'

export interface Props {
	frontmatter: Frontmatter
	canonicalUrl: URL
}

const frontmatter: Frontmatter = {
	description: '',
	title: 'bewebdev.tech - the best web development learning materials',
	layout: '',
}
---

<style is:global lang='scss'>
	a,
	button,
	input,
	select,
	textarea {
		&:focus-visible {
			outline: 4px solid var(--theme-accent);
			outline-offset: 4px;
		}

		&:focus:not(:focus-visible) {
			outline: none;
		}
	}
</style>

<html dir={frontmatter.dir ?? 'ltr'} lang={frontmatter.lang ?? 'en'}>
	<head>
		<HeadCommon />
		<LandingSEO />
		<title>
			{frontmatter.title ? `${frontmatter.title}` : CONFIG.SITE.title}
		</title>
	</head>
	<LandingPage />
</html>
